#! /bin/sh

# This is a shell script for starting the Comet Way Agent Browser.

CLASSPATH=${HOME}/classes:${HOME}/ak/ak.jar
java -classpath ${CLASSPATH} com.cometway.ak.AK -startup_agent com.cometway.swing.StartupEditor
