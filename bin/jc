#! /bin/sh

# This is a shell script for compiling java class files into 
# a common classes directory into your ~/classes directory.

echo Compiling...
ls $*
CLASSPATH=${HOME}/classes:${HOME}/ak.jar
javac -classpath ${CLASSPATH} -d ${HOME}/classes $*

